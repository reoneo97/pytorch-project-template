model_config:
  name: stack
  channels: 3
  n_classes: 10
  dim_sizes:
    - 4
    - 8
    - 16
    - 32
trainer_config:
  gpus: 1
  max_epochs: 15
  auto_lr_find: false
experiment_config:
  name: experiment-1
  train_path: "data/train"
  val_path: "data/val"
  lr: 0.03